#ifndef FOR_EACH_H
#define FOR_EACH_H
/*
#include "boost/typeof/typeof.hpp"

#define foreach(ITERATORVARNAME, CONTAINER) \
    for (typeof((CONTAINER).begin()) ITERATORVARNAME = (CONTAINER).begin(), CONCAT_TOKEN(ITERATORVARNAME, _END) = (CONTAINER).end(); ITERATORVARNAME != CONCAT_TOKEN(ITERATORVARNAME, _END); ++ITERATORVARNAME)
*/
#endif
